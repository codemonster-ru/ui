# Changelog

## Unreleased

- Added the package. It carries no layouts yet; `AdminLayout`, `AdminShell`, and `SetupLayout`
  arrive once the attribute contract that lets Razor express their state is designed.
