{
  "schemaVersion": 1,
  "name": "Tag",
  "slug": "tag",
  "razorTag": "cm-tag",
  "description": "An outlined inline label for categorizing content. Badge is the filled counterpart: a tone changes a Tag's outline and text while it changes a Badge's fill.",
  "props": {
    "tone": {
      "type": "enum",
      "required": false,
      "default": "neutral",
      "values": ["neutral", "primary", "success", "info", "warning", "help", "danger", "contrast"]
    }
  },
  "slots": {
    "default": { "required": true, "description": "Visible tag text or trusted inline content." }
  },
  "events": {},
  "states": ["default"]
}
